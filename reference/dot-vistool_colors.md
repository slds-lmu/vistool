# Color management for vistool

Unified color management system for consistent colors across ggplot2 and
plotly visualizations. Supports both discrete colors (for points,
traces, lines) and continuous colorscales (for surfaces).

## Usage

``` r
.vistool_colors
```

## Format

An object of class `list` of length 7.
