% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Objective.R
\name{objective_linear}
\alias{objective_linear}
\title{Linear regression objective}
\usage{
objective_linear(
  x,
  y,
  weights = NULL,
  lambda = 0,
  alpha = 0,
  lambda_l1 = NULL,
  lambda_l2 = NULL,
  include_intercept = TRUE,
  penalize_intercept = FALSE,
  loss_scale = NULL,
  id = NULL,
  label = NULL,
  transform = objective_transform_identity()
)
}
\arguments{
\item{x}{(\code{matrix()})
Design matrix without an intercept column. Rows correspond to observations.}

\item{y}{(\code{numeric()})
Numeric responses for the regression task.}

\item{weights}{(\code{numeric()})
Optional non-negative weights with length \code{nrow(x)}.}

\item{lambda}{(\code{numeric(1)})
Overall penalty strength used together with \code{alpha} for elastic net penalties.}

\item{alpha}{(\code{numeric(1)})
Elastic net mixing parameter in \verb{[0, 1]}. \code{0} corresponds to ridge, \code{1} to lasso.}

\item{lambda_l1}{(\code{numeric(1)})
Optional direct specification of the L1 penalty weight. Overrides \code{lambda * alpha} when provided.}

\item{lambda_l2}{(\code{numeric(1)})
Optional direct specification of the L2 penalty weight. Overrides \code{lambda * (1 - alpha)} when provided.}

\item{include_intercept}{(\code{logical(1)})
If \code{TRUE}, an intercept column is prepended to \code{x} before constructing the objective.}

\item{penalize_intercept}{(\code{logical(1)})
If \code{FALSE} and \code{include_intercept = TRUE}, the intercept term is excluded from the penalties.}

\item{loss_scale}{(\code{numeric(1)})
Scaling factor applied to the summed squared errors. Defaults to \code{1 / (2 * sum(weights))}.}

\item{id}{(\code{character(1)})
Identifier forwarded to \link{Objective}. Defaults to \code{"logreg"}.}

\item{label}{(\code{character(1)})
Label used for the resulting objective. Defaults to \code{"logistic risk"}.}

\item{transform}{(\code{objective_transform()})
Optional scalar transformation applied to the empirical risk before it is
returned. Defaults to \code{\link[=objective_transform_identity]{objective_transform_identity()}}.}
}
\value{
An \link{Objective} instance capturing the squared-error regression risk.
}
\description{
Builds a squared-error regression objective with optional elastic net penalization.
}
